public enum CurrentController
{
    Playstation,
    Xbox,
    Keyboard,
    Mouse
}

public enum CharacterType
{
    Player,
    AI
}

public enum FlipMode
{
    MovementDirection,
    LookDirection
}
